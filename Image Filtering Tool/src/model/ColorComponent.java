package model;

/**
 * represents one of a set number of color components which are understood by the
 *   Pixel, SimpleImage, and ImageProcessorModel.
 */
public enum ColorComponent {
  Red, Green, Blue, Value, Intensity, Luma
}
